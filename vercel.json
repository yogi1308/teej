{
    "buildCommand": "npx prisma generate --schema=server/prisma/schema.prisma && npm run build",
    "outputDirectory": "dist",
    "rewrites": [
        { "source": "/api/(.*)", "destination": "/api" },
        { "source": "/((?!api).*)", "destination": "/index.html" }
    ]
}
